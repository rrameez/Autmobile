The scikit version is 0.18.1

Python version is 3.6.1

The answers are in the DS Challenge.docx file

In case you don't want to run the code, the results
are in Results.docx

The predictedvsActual is a graph for showing the little
difference between predicted and actual values and why
MAE rather than MSE is used as an evaluation metric